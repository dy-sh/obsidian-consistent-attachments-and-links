{
  // Scoped markdownlint config for the demo vault. Only one default rule is incompatible with what
  // These notes do; every other rule applies, as it does to the rest of the documentation.
  "config": {
    // A note here deliberately keeps an Obsidian `[[wikilink]]` -- see the
    // `obsidian-dev-utils-disable` directives and the reasons they carry -- and markdownlint parses
    // One as an undefined shortcut reference link.
    "MD052": false
  }
}
